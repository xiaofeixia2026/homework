# 个人简介网页作业

## 一、作业概述
本次作业的主要目标是创建一个静态的 HTML 个人简介网页。
## 二、使用的 HTML 标签
### （一）文档结构标签
1. `<!DOCTYPE html>`：声明文档类型为 HTML5，告知浏览器以 HTML5 标准来解析页面。
2. `<html>`：HTML 文档的根标签，`lang="zh-CN"` 属性指定页面语言为中文。
3. `<head>`：包含文档的元数据，如字符编码和页面标题。
4. `<meta charset="UTF-8">`：设置页面字符编码为 UTF - 8，确保中文等字符能正确显示。
5. `<title>`：定义页面在浏览器标签栏显示的标题。
6. `<body>`：包含页面的可见内容，如文本、图像、链接等。

### （二）文本标签
1. `<h1>`、`<h2>`、`<h3>`：分别用于定义一级、二级和三级标题，用于区分页面的不同层次内容。
2. `<p>`：用于定义段落文本。
3. `<strong>`：用于强调文本，通常显示为粗体。
4. `<em>`：用于表示强调，通常显示为斜体。

### （三）分隔与装饰标签
`<hr>`：插入一条水平线，用于分隔不同的内容模块。

### （四）图像标签
`<img>`：用于在页面中插入图像，`src` 属性指定图像的源地址，`alt` 属性提供图像加载失败时的替代文本。

### （五）链接标签
`<a>`：用于创建超链接，`href` 属性指定链接的目标地址，`target="_blank"` 属性使链接在新窗口中打开。

## 三、遇到的问题及解决方案

### （一）图片显示问题
在使用 `<img>` 标签插入图片时，一开始使用了本地图片路径，但在不同的环境中无法正常显示。
**解决方案**：采用了网络图片链接（如 `https://picsum.photos/200/200`），确保图片能在不同环境下都能正确显示。如果要使用本地图片，需要注意使用相对路径，保证在项目目录结构改变时图片链接依然有效。

### （二）链接打开方式问题
在设置 `<a>` 标签链接时，一开始没有添加 `target="_blank"` 属性，导致点击链接后在当前页面跳转，影响用户体验。
**解决方案**：在 `<a>` 标签中添加 `target="_blank"` 属性，使链接在新窗口中打开，避免页面跳转对用户当前浏览内容的干扰。

### （三）代码格式问题
在编写代码过程中，偶尔会出现标签写混大小写、属性值未加双引号等不规范的情况，影响代码的可读性和规范性。
**解决方案**：编写时严格按照 HTML 规范，统一使用小写标签和属性，给属性值加上双引号，同时使用代码编辑器（如 VSCode）的自动格式化功能，确保代码格式工整，子标签缩进规范。 